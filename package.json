{
  "name": "cartas-duplas-owlbear",
  "version": "0.1.0",
  "author": "DemonRider",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build.mjs",
    "build:preset-decks": "node scripts/build-preset-decks.mjs && node scripts/build-preset-cards.mjs",
    "build:preset-cards": "node scripts/build-preset-cards.mjs",
    "build:scene-presets": "node scripts/build-scene-preset-index.mjs",
    "audit:assets": "node scripts/audit-assets.mjs",
    "check": "node scripts/check-project.mjs",
    "test:regressions": "node scripts/test-regressions.mjs",
    "prepare:github-assets": "node scripts/prepare-github-assets.mjs"
  },
  "dependencies": {
    "@owlbear-rodeo/sdk": "3.1.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.3",
    "rollup": "^4.60.4"
  }
}
